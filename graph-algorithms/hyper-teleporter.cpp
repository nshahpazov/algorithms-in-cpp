#include <iostream>
#include <map>
#include <stdio.h>

using namespace std;

int main()
{
    scanf("%d %d %d", &n, &m, &k);
    graph.resize(n + 1);
    for (int i = 0; i < n; i++)
    {
        char name[256];
        scanf("%s", &name);
    }

    return 0;
}
